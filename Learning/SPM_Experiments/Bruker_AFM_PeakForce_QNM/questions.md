## Some questions for Tamas
1. (Done)  The `Data offset` and `Data length` fields inside the header describe how an equivalent Bruker binary `.spm` file would encode the same data
1. ()  No Z-offset is available.
1. ()  No Z-start and Z-end are available. Though I can get them from array.